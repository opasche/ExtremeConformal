% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Conformal_EQR.R
\name{conformalize_EQR_unilat}
\alias{conformalize_EQR_unilat}
\title{Single-sided extreme conformal prediction}
\usage{
conformalize_EQR_unilat(
  EQR_pred,
  y_calibr,
  coverage_proba = NULL,
  coverage_alpha = NULL,
  method = c("GPD_safeprofile", "GPD_boot", "GPD_profile", "Empirical",
    "GPD_profile_naive", "GPD_delta", "GPD_max", "GPD_simple", "Hill"),
  threshold_lvls = NULL,
  alpha_correction = c("Sidak", "Bonferroni"),
  correction_prop = 0.5,
  R = 1004,
  min_obs_GPD = 10,
  profile_init_step_pos = 100,
  profile_init_step_neg = 10,
  profile_tol = 0.001,
  profile_steps_beyond_conf = 5,
  profile_max_steps = 1000,
  alpha_profile_naive = 0.01,
  return_q_list = FALSE,
  verbose = 1
)
}
\arguments{
\item{EQR_pred}{Vector of extreme quantile predictions for the calibration data,
ideally at level \code{coverage_proba} (same length as \code{y_calibr}).}

\item{y_calibr}{Vector of observed response values of the calibration data (same length as \code{EQR_pred}).}

\item{coverage_proba, coverage_alpha}{Marginal coverage probability (or level alpha) for the conformal prediction interval.
Only one of \code{coverage_proba} or \code{coverage_alpha} must be provided, as \code{coverage_alpha = 1 - coverage_proba}.}

\item{method}{Method for constructing the conformal prediction interval. Defaults to \code{'GPD_safeprofile'}.
See the Details section for more information.}

\item{threshold_lvls}{Threshold probability level for the GPD-based methods.
The \code{'GPD_max'} model instead requires a vector of threshold levels.
Defaults to 0.95 (or to a sequence from 0.8 to 0.99 with the \code{'GPD_max'} method).}

\item{alpha_correction}{Confidence correction method for the CI-based extreme conformal prediction intervals
used to choose \eqn{\alpha_1} and \eqn{\alpha_2}. Defaults to \code{'Sidak'}.}

\item{correction_prop}{Relative (pseudo-)proportion between the values of \eqn{\alpha_1} and \eqn{\alpha_2},
for the CI-based extreme conformal prediction intervals. Defaults to 0.5 (equal values of \eqn{\alpha_1} and \eqn{\alpha_2}).
Larger values increase \eqn{\alpha_1} and decrease \eqn{\alpha_2}.}

\item{R}{Number of bootstrap replicates for the \verb{''GPD_boot'} method.}

\item{min_obs_GPD}{Minimum number of observations above the threshold for the GPD-based methods.}

\item{profile_init_step_pos, profile_init_step_neg}{Initial binary-search step size, in the positive and negative directions,
for the profile-likelihood methods. See \code{\link[ExtremeCI:GPD_profile_CI]{ExtremeCI::GPD_profile_CI()}} for more details.}

\item{profile_tol}{Tolerance for the profile-likelihood search. See \code{\link[ExtremeCI:GPD_profile_CI]{ExtremeCI::GPD_profile_CI()}} for more details.}

\item{profile_steps_beyond_conf}{Number of safety steps beyond the confidence line for the profile-likelihood search.
See \code{\link[ExtremeCI:GPD_profile_CI]{ExtremeCI::GPD_profile_CI()}} for more details.}

\item{profile_max_steps}{Maximum number of initial search steps for the profile-likelihood CIs.
See \code{\link[ExtremeCI:GPD_profile_CI]{ExtremeCI::GPD_profile_CI()}} for more details.}

\item{alpha_profile_naive}{CI confidence level (i.e., the equivalent of \eqn{\alpha_2}) for the \code{'GPD_profile_naive'} method.}

\item{return_q_list}{Boolean indicating whether to return a debug quantile list.}

\item{verbose}{Verbose level (0 for no messages, 1 for warnings, 2 for warnings duplicated as \code{\link[=cat]{cat()}} prints).}
}
\value{
A named list containing the following elements.
\item{dQ}{The conformal correction \eqn{\hat{q}^{e}_{\alpha}} or \eqn{\hat{q}_{\alpha}} to be added to the extreme quantile prediction(s) to obtain the conformal PI endpoint.}
\item{coverage_proba}{The marginal coverage probability for the conformal PI.}
\item{coverage_alpha}{The marginal coverage alpha level for the conformal PI.}
\item{threshold_lvls}{The threshold level(s) effectively used for the GPD-based methods.}
\item{method}{The \code{method} used to obtain the conformal PI.}
\item{dQ_thresholds}{A debugging vector of quantiles at the specified threshold levels. Only for GPD-based methods when \code{return_q_list==TRUE}.}
\item{sigma}{The GPD scale parameter estimate. Currently only for method 'GPD_simple', when \code{return_q_list==TRUE}.}
\item{xi}{The GPD shape parameter estimate. Currently only for method 'GPD_simple', when \code{return_q_list==TRUE}.}
}
\description{
The main function of this package, performing single-sided extreme conformal prediction from extreme quantile regression predictions.
This function computes the conformal correction score-quantile \eqn{\hat{q}^{e}_{\alpha}} (or \eqn{\hat{q}_{\alpha}})
to be added to the extreme quantile regression prediction(s) to obtain the desired PIs.
}
\details{
The \code{method} argument specifies the conformalization method used to construct the (extreme) conformal prediction intervals (PIs).
The available method options are:
\describe{
\item{'GPD_safeprofile'}{Recommended choice for extreme conformal prediction.
Tries the method 'GPD_profile' first, and falls back to 'GPD_boot' if the former suffers from numerical instability.}
\item{'GPD_profile'}{GPD-based extreme conformalization using the profile-likelihood CI endpoint of the extreme score quantile.
It captures the asymetric uncertainty of the score-quantile best, yielding the most reliable coverage.
It might sometimes overcover or suffer from numerical convergence issues.}
\item{'GPD_boot'}{GPD-based extreme conformalization using the nonparametric bootstrap percentile CI endpoint of the extreme score quantile.}
\item{'GPD_delta'}{GPD-based extreme conformalization using the Delta method CI endpoint of the extreme score quantile.}
\item{'Empirical'}{The classical (non-extreme) conformalized quantile regression method, relying on the empirical quantile of the nonconformity scores.
Yields infinitely wide PIs if the \code{coverage_proba} is larger than \verb{1-1/(length(y_calibr)+1}).}
\item{'GPD_profile_naive'}{A naive version of \code{'GPD_profile'}, without the \code{alpha_correction} for multiple testing.
Is likely to undercover, only use for comparison.}
\item{'GPD_max'}{A naive GPD-based approach repeating the \code{'GPD_simple'} method for a range of threshold levels, keeping the most conservative results.
Is likely to undercover, only use for comparison.}
\item{'GPD_simple'}{A naive GPD-based extreme conformalization using a simple extrapolated score quantile estimate instead of a CI endpoint.
Is likely to undercover, only use for comparison.}
\item{'Hill'}{(Not implemented) Extreme conformalization based on the Hill estimator from extreme value analysis.}
}
See Pasche et al. (2025), referenced below, for the technical details of extreme conformal prediction.
}
\references{
Pasche, O. C., Lam, H., and Engelke, S. (2025). "Extreme Conformal Prediction: Reliable Intervals for High-Impact Events." \emph{ArXiv Preprint}. \doi{doi:10.48550/arXiv.2505.08578}.
}
